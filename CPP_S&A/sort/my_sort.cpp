#include "my_sort.hpp"

template<typename T> 
T GetNthElement(const T *p_first ,int element_num ,int index)
{
	
}

template<typename T> 
bool BubbleSortAssend(T *p_first , int ele_num)
{
	T buff;
	for (int i = 0; i < ele_num-1 ; i++) {
		for (int j = 0; j < ele_num-i-1; j++) {
						
		}
	}

}
